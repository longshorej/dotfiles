# likely flawed, but try to keep this file compatible with bash/zsh
# @TODO maybe simply switch back to bash?

alias cgd="clear; gd"
alias cgds="clear; gds"
alias cll="clear; ll"
alias gaa="git add -A"
alias gbs="git branch --show"
alias gbls="git for-each-ref --sort=committerdate refs/heads/"
alias gd="git diff"
alias gds="git diff --staged"
alias gl="git log"
alias gs="git status"
alias ll="ls -alF"

